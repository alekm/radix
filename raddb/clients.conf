# Add a stanza per AP vendor/subnet as needed.
# RADIUS_SECRET is injected from the environment at runtime.

client all_aps {
    ipaddr                        = 0.0.0.0/0
    secret                        = $ENV{RADIUS_SECRET}
    shortname                     = aps
    nas_type                      = other
    require_message_authenticator = true
}
